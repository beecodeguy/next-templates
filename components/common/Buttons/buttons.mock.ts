import { IBaseButtonProps } from "components/common/Buttons/Buttons";

const baseExampleString: IBaseButtonProps = {
  btnText: "Hello World",
  backgroundColor: "coral",
};

export const baseTemplateProps = {
  baseExampleString,
};
